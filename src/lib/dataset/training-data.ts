/**
 * Training Data (default entry-point)
 *
 * `training-data-v2` was promoted to the default dataset module.
 * Keep re-exports centralized here so consumers no longer depend on
 * versioned file names.
 */

export * from "./training-data-v2";
